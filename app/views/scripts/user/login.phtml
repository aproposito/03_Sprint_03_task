<script src="https://cdn.tailwindcss.com"></script>

<!-- Container -->
<div class="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100 flex items-center justify-center p-4">
  <div class="w-full max-w-md bg-white rounded-lg shadow-lg p-8">
    
    <!-- Tabs Header -->
    <div class="flex gap-2 mb-8">
      <a 
        href="<?= $this->baseUrl() ?>/user/login"
        class="flex-1 py-2 px-4 text-center font-semibold rounded-lg transition-colors bg-indigo-600 text-white hover:bg-indigo-700">
        Login
      </a>
      <a 
        href="<?= $this->baseUrl() ?>/user/register"
        class="flex-1 py-2 px-4 text-center font-semibold rounded-lg transition-colors bg-gray-200 text-gray-700 hover:bg-gray-300">
        Register
      </a>
    </div>

    <!-- Login Form -->
    <h1 class="text-2xl font-bold text-gray-800 mb-6">Welcome Back! 👋</h1>
    
    <?php if ($this->error): ?>
      <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded-lg mb-4">
        <?= htmlspecialchars($this->error) ?>
      </div>
    <?php endif; ?>

    <form method="POST" action="<?= $this->baseUrl() ?>/user/login">
      <!-- Username Input -->
      <div class="mb-5">
        <label class="block text-gray-700 font-semibold mb-2">Username</label>
        <input 
          type="text" 
          name="username" 
          required
          placeholder="Enter your username"
          class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-transparent transition">
      </div>

      <!-- Password Input -->
      <div class="mb-6">
        <label class="block text-gray-700 font-semibold mb-2">Password</label>
        <input 
          type="password" 
          name="password" 
          required
          placeholder="Enter your password"
          class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-transparent transition">
      </div>

      <!-- Submit Button -->
      <button 
        type="submit"
        class="w-full bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-2 px-4 rounded-lg transition-colors duration-200">
        Login
      </button>
    </form>

  </div>
</div>