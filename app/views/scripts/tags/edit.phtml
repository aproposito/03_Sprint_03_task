<div class="max-w-sm mx-auto mt-8 p-4">

    <!-- Header -->
    <div class="flex justify-between items-center mb-6">
        <h1 class="text-xl font-bold">Edit tags</h1>
        <a href="<?= $this->baseUrl() ?>/tags" class="text-sm text-gray-500">← go back</a>
    </div>

    <form action="<?= $this->baseUrl() ?>/tags/edit/<?= $this->tag['id'] ?>" method="POST" class="flex flex-col gap-3">

        <!-- Name -->
        <input type="text" name="name" value="<?= $this->tag['name'] ?>" required
            class="border border-gray-300 rounded px-3 py-2 text-sm w-full"/>

        <!-- Color picker -->
        <div>
            <p class="text-sm text-gray-500 mb-1">color</p>
            <div class="flex flex-wrap gap-2">
                <?php
                $colors = [
                    '#6B7280', '#86EFAC', '#1D9E75', '#F9A8D4', '#8B5CF6',
                    '#EF4444', '#93C5FD', '#3B8BD4', '#FDE047', '#F97316',
                    '#1E3A5F', '#D4A017', '#14B8A6', '#F87171', '#E9D5FF'
                ];
                foreach ($colors as $color): ?>
                    <label>
                        <input type="radio" name="color" value="<?= $color ?>"
                            <?= $this->tag['color'] === $color ? 'checked' : '' ?>
                            class="hidden peer"/>
                        <span class="w-7 h-7 rounded-full block cursor-pointer ring-2 ring-transparent peer-checked:ring-black"
                            style="background-color:<?= $color ?>"></span>
                    </label>
                <?php endforeach ?>
            </div>
        </div>

        <!-- Icon picker -->
        <div>
            <p class="text-sm text-gray-500 mb-1">icon</p>
            <div class="flex flex-wrap gap-2">
                <?php
                $icons = ['#️⃣', '🏠', '🚗', '🌼', '🍽️', '🎂', '✈️', '💼', '📚', '🎵', '🏋️', '💊', '🛒', '🎮', '🌟'];
                foreach ($icons as $icon): ?>
                    <label>
                        <input type="radio" name="icon" value="<?= $icon ?>"
                            <?= $this->tag['icon'] === $icon ? 'checked' : '' ?>
                            class="hidden peer"/>
                        <span class="w-8 h-8 flex items-center justify-center rounded-full border-2 border-transparent peer-checked:border-black cursor-pointer text-lg">
                            <?= $icon ?>
                        </span>
                    </label>
                <?php endforeach ?>
            </div>
        </div>

        <!-- Buttons -->
        <div class="flex justify-between mt-2">
            <a href="<?= $this->baseUrl() ?>/tags/delete/<?= $this->tag['id'] ?>"
                class="bg-red-500 text-white text-sm px-4 py-2 rounded">Delete</a>
            <button type="submit"
                class="bg-white border border-gray-300 text-sm px-4 py-2 rounded">Save</button>
        </div>

    </form>

</div>