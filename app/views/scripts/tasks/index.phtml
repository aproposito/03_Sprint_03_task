<div class="max-w-2xl mx-auto mt-12 px-4">
    <div class="flex items-center justify-between mb-6">
        <h1 class="text-xl font-medium text-gray-800">My tasks</h1>
        <div class="flex gap-3 text-sm">
            <a href="<?= $this->baseUrl ?>/tasks/create" class="text-gray-800 font-medium hover:underline">New task</a>
            <a href="<?= $this->baseUrl ?>/tags" class="text-gray-800 font-medium hover:underline">Manage tags</a>
            <a href="<?= $this->baseUrl ?>/user/profile" class="text-gray-400 hover:text-gray-600">Profile</a>
            <a href="<?= $this->baseUrl ?>/user/logout" class="text-gray-400 hover:text-gray-600">Logout</a>
        </div>
    </div>

    <form action="<?= $this->baseUrl ?>/dashboard" method="GET"
        class="flex flex-wrap gap-2 mb-6">
        <input name="search" type="text" placeholder="Search..."
            class="flex-1 min-w-32 px-3 py-2 text-sm border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-gray-300">
        <select name="status"
            class="px-3 py-2 text-sm border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-gray-300 text-gray-600">
            <option value="">All statuses</option>
            <option value="pending">Pending</option>
            <option value="in_progress">In progress</option>
            <option value="completed">Completed</option>
        </select>
        <select name="tags"
            class="px-3 py-2 text-sm border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-gray-300 text-gray-600">
            <option value="">All tags</option>
            <?php foreach ($this->allTags as $tag): ?>
                <option value="<?= $tag['id'] ?>"
                    <?= in_array($tag['id'], $this->selectedTagIds ?? []) ? 'selected' : '' ?>>
                    <?= $tag['name'] ?>
                </option>
            <?php endforeach ?>
        </select>
        <button type="submit"
            class="px-4 py-2 text-sm font-medium bg-gray-100 hover:bg-gray-200 text-gray-800 rounded-lg transition-colors">
            Search
        </button>
        <a href="<?= $this->baseUrl ?>/dashboard"
            class="px-4 py-2 text-sm text-gray-400 hover:text-gray-600 rounded-lg">
            Clear
        </a>
    </form>

    <ul class="space-y-2">
        <?php foreach ($this->tasks as $task): ?>
            <li class="flex items-start justify-between gap-4 px-4 py-3 bg-white border border-gray-200 rounded-lg">
                <div class="flex-1 min-w-0">
                    <p class="text-sm font-medium text-gray-800"><?= $task['name'] ?></p>
                    <?php if ($task['description']): ?>
                        <p class="text-xs text-gray-400 mt-0.5 line-clamp-3"><?= htmlspecialchars($task['description']) ?></p>
                    <?php endif ?>
                    <div class="flex flex-wrap gap-1.5 mt-1.5">
                        <span class="text-xs text-gray-400"><?= $task['status'] ?></span>
                        <?php foreach ($task['tags'] as $tag): ?>
                            <span class="text-xs bg-gray-100 text-gray-600 px-2 py-0.5 rounded-full">
                                <?= $tag['icon'] ?> <?= $tag['name'] ?>
                            </span>
                        <?php endforeach ?>
                    </div>
                </div>
                <div class="flex gap-3 text-xs shrink-0 mt-0.5">
                    <a href="<?= $this->baseUrl ?>/tasks/edit/<?= $task['id'] ?>" class="text-gray-500 hover:underline">View</a>
                    <a href="<?= $this->baseUrl ?>/tasks/delete/<?= $task['id'] ?>" class="text-red-400 hover:underline">Delete</a>
                </div>
            </li>
        <?php endforeach ?>
    </ul>
</div>